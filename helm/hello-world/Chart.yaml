apiVersion: v2
name: hello-world
description: Hello World Node.js app — thin wrapper around the shared nodejs-app chart
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: nodejs-app
    version: "0.1.0"
    # Local-testing placeholder — only resolves when helm-nodejs-app is
    # checked out as a sibling directory on the same filesystem. Swap for a
    # real chart repo / OCI registry (e.g. oci://<account>.dkr.ecr.<region>.amazonaws.com/helm-charts)
    # before relying on this in a real Jenkins pipeline — see
    # ../../helm-nodejs-app/README.md "Versioning".
    repository: "file://../../../helm-nodejs-app"
